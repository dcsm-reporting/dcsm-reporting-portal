node_modules/
dist/
.wrangler/
.dev.vars
*.local
.DS_Store
coverage/
*.log

.wrangler-dev.log
.friends-sync-secret.txt

# local backup dumps — commit deliberately with `git add -f` if wanted
backups/
