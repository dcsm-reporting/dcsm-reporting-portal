node_modules/
dist/
.wrangler/
.dev.vars
*.local
.DS_Store
coverage/
*.log

.wrangler-dev.log
.friends-sync-secret.txt

# local backup dumps — commit deliberately with `git add -f` if wanted
backups/

# Source workbooks and the legacy baptism list carry members' and friends'
# personal data. They stay on the office machine / mission Drive, not in git.
resources/*.xlsx
resources/wdcs_legacy_baptisms.csv
